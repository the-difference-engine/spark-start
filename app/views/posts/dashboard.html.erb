<h1>Dashboard</h1>
<br>
<h2>Posts</h2>

<% number = 1 %>
<% @posts.each do |post| %>
  <p><%= number %> <b><%= link_to post.title, "/blog/#{post.id}" %></b> By <%= post.author %>, <%= post.created_at.strftime("%m-%e-%y %H:%M") %></p>
  <p><%= link_to "Edit", "/blog/#{post.id}/edit" %> <%= link_to "Delete", "/blog/#{post.id}", method: :delete %></p>
  <% number +=1 %>
  <br>
<% end %>

<h2>Categories</h2>

<% @categories.each do |category| %>
  <b><%= category.name %></b><br>
  <% category.posts.each do |post| %>
    <%= link_to post.title, "/blog/#{post.id}" %><br>
  <% end %><br>
<% end %>

<h2>Tags</h2>

<% @tags.each do |tag| %>
  <b><%= tag.tag_name %></b><br>
  <% tag.posts.each do |post| %>
    <%= link_to post.title, "/blog/#{post.id}" %><br>
  <% end %><br>
<% end %>